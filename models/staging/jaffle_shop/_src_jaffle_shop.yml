sources:
  - name: jaffle_shop
    schema: main
    tables:
      - name: raw_customers
      - name: raw_order_items
      - name: raw_products
      - name: raw_stores
      - name: raw_supplies
      - name: raw_orders
        config:
          freshness:
            warn_after:
              count: 3650
              period: day # For learning purposes — data is static
            error_after: 
              count: 365000
              period: day # Never fail at a leraning project
          loaded_at_field: ordered_at 
